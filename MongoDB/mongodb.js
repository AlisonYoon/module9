//Mongo DB
// It is not a relational DB
// different way of thinking about db document storage
// store JSON in Mongo (it's the format you want from db. make things faster)
// document : JSON object stored in the DB
// collection : array of objects/documents
// it's the leading NoSQL(Not Only SQL) db around.
// it's very flexible, there's no schema

// run command :
//$ mongod --config /usr/local/etc/mongod.conf

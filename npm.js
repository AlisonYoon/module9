// NPM : node package manager
// publicly available packages
// Add node module in you .gitignore
// in package.json file, it has all the dependencies a project needs. So when do $npm install, this will get all the npm packages it needs
